/target
Cargo.lock
**/*.rs.bk
.lefthook/
.DS_Store
.devin/

# Local agent / tooling artifacts (not version-controlled)
.playwright-mcp/

# Audit / debug log output (generated, never committed)
/logs/
*.log
*.audit.md

# Combined set card files — GENERATED from the per-card source files
# (cards/<set>/<collector>.toml) by cards/scripts/combine_sets.py. The engine
# loads these and the wasm crate embeds them, so CI/local must regenerate them
# before building or testing. Never committed.
/cards/sets/

# Local-model card-drafting worktables (generated by cards/scripts/dsl_author.py)
cards/scripts/drafted_abilities.toml
cards/scripts/drafted_skips.tsv
